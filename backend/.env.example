# Comma-separated origins. Use * for a quick local-only setup.
FRONTEND_URL=http://localhost:5173

# Railway volume mount path. Local development defaults to ./data/fieldnotes.db.
DATABASE_PATH=./data/fieldnotes.db
PORT=3000
